# Tauri 2.9.x 兼容配置 - 已移除Windows 7限制

[target.x86_64-pc-windows-msvc]
rustflags = [
    # 允许新的Windows API特性
    "-C", "target-feature=-crt-static",
]

[env]
# 使用现代Windows API (Windows 10)
WINVER = "0x0a00"
_WIN32_WINNT = "0x0a00"